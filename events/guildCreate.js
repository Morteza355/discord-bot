module.exports = {
    name: 'guildCreate',
    async execute(guild) {
        console.log(guild);
        // client.user.setActivity(` /help || ${ArrayGuilds.length} Servers`, { type: 'PLAYING' });
    },
};